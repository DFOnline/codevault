{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "codevault",
            "database": "${workspaceFolder:codevault}/dist/data.db"
        }
    ]
}